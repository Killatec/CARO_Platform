# CARO_HMI Trend Viewer — Spec Deltas

**Purpose:** One-line entries for divergences from `hmi_trend_viewer_spec.md` during implementation. Propagate to target docs at session end, then delete entries. The file should be empty or near-empty between sessions.

---

**Last cleared:** 2026-05-17. Audit remediation pass closure — M1 (signature change + meta + singleton removal), D1 (top-level summary log), F5 (per-tag outbox cap), TG-7 follow-up (`commitAndDrain` fix), and TG-1 (chunk-pruning regression test) propagated to spec §4.1 / §4.4 / §14.7 and handoff §1 / §10. `Docs/Update_plan.md` deleted; audit history in git: commits through 2026-05-17.

**Last cleared:** 2026-05-18. Phase 2 implementation bug-fix arc closure — five architectural decisions from the post-Phase-2 fix cascade propagated to handoff §10 divergences 25–29 and §5 / §8 invariants: (25) `spineMetadataMatches` gates the live-wins clip in `seedFromSpineFetch`; (26) wholesale replace clears accumulators / rawBuffers / sessionHighWaterMark synchronously; (27) type-coherence guard in `getBufferSnapshot`; (28) split live/history paths for `mergedData` in `TrendChartContainer`; (29) `lastChartDataRef` bridges chart data across transitions. Commits: 4763614 (initial 3 fixes), 6ffa8ee (Phase 2b cutover), Phase 2c trim, e9f10dd (wholesale-replace clear), e8b926f (diagnostic cleanup).

**Last cleared:** 2026-05-19. Phase 4/5/6 implementation arc propagated: orange-button refinement (spec §12.3, handoff §3/§7); `isFreshLiveLanding` scoping to `live-fixed → live-trailing` only (handoff §3); symmetric window-vs-live-edge rule superseding D6/D7 with `classifyByWindow` helper (spec §9.3/§12.2, handoff §3/§10 div.30); `syncDataViewport` added to `!wasLive && willBeLive` branch — Phase 5 (handoff §5/§10 div.31); `syncDataViewport` hoisted unconditionally to top of `dispatchModeAction` — Phase 6 (handoff §7/§10 div.31). Three-state mode machine, unified Live buffer, and revised glossary also propagated to spec §9.3/§10.6–§10.8/§12.1–§12.4/§13.1/§14.4/§19 and handoff §3/§5/§7/§8. Phase B residual issues E/F/G captured in handoff §11.

**Last cleared:** 2026-05-21. Unified-tile doc reconciliation. H1 (`showLastWhenIdle` → `isLive`; already matched spec §8.4) and H2 (diagnostic `console.warn` cleanup; no spec impact) — resolved, no propagation needed. H3 (v1.6 run-independent resolve flow) and H4 (`drainBuffers` keeps the ring on live→fixed) — propagated into the spine→unified-tile rewrite of spec §10.6–§10.8 and handoff §5/§8/§10. H5 (Legend raw-cursor union-grid index resolution) — propagated to a handoff Gotcha. The full spec + handoff reconciliation to the as-built unified-tile architecture landed the same day. (Note: these H1–H5 are delta-file-local labels, distinct from the 2026-05-19 trend-viewer audit's own H/M-series finding labels.)

**Last cleared:** 2026-05-21. Legend cursor-value resolution rewrite. The `Legend` per-tag value is now resolved from the cursor's X-axis timestamp (`cursorTsMs` prop, replacing `cursorIdx`): raw uses LOCF seeded by `prev` with a union-extent upper bound; aggregate uses `floor((cursorTsMs−startTime)/bucketSMs)`. Propagated to spec §8.5 (per-tag cursor-value rule, plus a correction to the cursor-time-field description — `posToVal`, not a bucket snap) and the handoff §10 Gotcha, which supersedes the earlier H5 union-grid-index Gotcha (that approach was an intermediate fix, replaced by timestamp resolution). The delivered-features checklist wording was also corrected (`Cursor:` field label, "at the cursor").

**Last cleared:** 2026-05-26. Five entries propagated: (1) `responseTailTs` → `committedThroughTs` rename + `DbPipeline` watermark semantics + trim-threshold fudge removal → spec §6.2/§10.6/§10.8, handoff §2/§5/§7/§8, API spec §7, platform_handoff apps+packages tables; (2) `mergeTrendData` `totalN` cap → spec §10.6; (3) `querySegment` absent-tag bounded-prev fallback → spec §5.5; (4) `invalidateNonTerminalTiles` Fixed→Live edge + `dispatchModeAction` second side effect → spec §10.6/§10.7, handoff §5/§7/§10 div.31–33; (5) `needsFetch` live branch collapsed to `return false` → spec §10.6, handoff §5. Spec bumped to v1.9; API spec to v1.8. Test count 694 → 698 across handoff §1.

**Last cleared:** 2026-05-24. Unified-viewport + storm-fix arc — six entries propagated: (1) `mergeAggregate` negative-`liveStartIndex` guard → spec §10.6; (2) `dataViewport` removal + `modeViewport`/`bucketSMs` drive → spec §9.3/§10.5/§10.6, handoff §4/§5/§7; (3) `tilesForViewport` explicit `tileSpanMs` + left-anchor + variable 2–4 tile count → spec §10.5, handoff §2; (4) `handleZoomLevelSwitch` pure resolution change → spec §9.2/§9.3; (5) `useZoomState` render-time derivation for non-zoom intents → spec §9.3/§10.5, handoff §4; (6) `MAX_VISIBLE_TILES = 16` cap → spec §10.5, handoff §2. `hmi_trend_viewer_first_tile_bug_handoff.md` deleted (misdiagnosed; superseded by the unified-tile and unified-viewport architecture). Test count updated 586 → 694 across handoff §1, platform_handoff apps table and packages table.

**Last cleared:** 2026-05-26. Four entries propagated: (1) Y-axis `Y_AXIS_SIZE_PX=60` / `Y_AXIS_LABEL_SIZE_PX=16` + label=`' '` fallback → spec §8.1, handoff §2 `uplotConfig.ts`, platform_handoff `@caro/trend-chart` row; (2) Legend `tag_name` label source + STRIP sizing → spec §8.4 (already reflected), handoff §2 `Legend.tsx`; (3) Legend 5-col table + `format` plumbing + `formatValue` signature → spec §8.4 (already reflected), handoff §2 `Legend.tsx` + `formatValue.ts`; (4) Label reorg (`CursorDisplay` range-message-only, cursor row into `Legend`, `SpanIndicator`/`BucketFetchIndicator` split, container cursor-state removal) → spec §8.5 + §8.6, handoff §2 file map (Legend, CursorDisplay, TrendChartContainer, TrendChart, new components, test entries), handoff §7 Container Wiring, handoff §1 test count 698→730, platform_handoff `@caro/trend-chart` row.

**Last cleared:** 2026-05-27. Step 12 (Tag Picker) Phase 4 + polish + cleanup propagated: spec §11 rewritten (§11.1–§11.6); handoff §1 (Step 12 row + test counts 752/67/156/301/33), §2 (Legend.tsx + TagPickerModal.tsx file map), §7 (picker wire point), §10 (divergences 34–38), §12 (Step 12 row removed); platform_handoff apps + packages tables updated; Platform Rules `trendable` added; `apps/caro-hmi/CLAUDE.md` @source note added; platform_todo.md Step 12 removed.

**Last cleared:** 2026-05-27. Post-Step-12 fix arc — two entries propagated: (39) `commitTagIds` single-mutation contract (modal OK + legend remove both call `invalidateNonTerminalTiles()` before `setTagIds()`) → spec §10.7 Add/Remove paragraphs + §11.3 OK-commit; handoff §10 div.39; handoff div.34 updated; platform_handoff `@caro/trend-chart` row. (40) Empty-tagIds full-layout preservation (gear icon; no "Add tags" button) → spec §13.5 rewritten; handoff §10 div.40; platform_handoff `@caro/trend-chart` row. Spec bumped to v2.1. Test count 752 → 770 across handoff §1.

**Last cleared:** 2026-05-27. Session persistence entry propagated → spec §13.6 added (localStorage key format, V1 schema, write cadence, hydration reconciliation rules, relationship to Saved Views); revision history bumped to v2.2; platform_handoff `@caro/trend-chart` row updated (persistence clause + test count 770 → 815).

**Last cleared:** 2026-05-27. 2026-05-27 audit propagation pass (Issues 2–10 from `Docs/hmi_trend_viewer_audit_2026-05-27.md`): (Issue 2) handoff Gotcha "`drainBuffers` must reset per-tag arrays in place" rewritten to cover both `rawBuffersRef` AND `accumulatorsRef` in-place reset patterns; accumulator fix attributed to the 2026-05-27 audit pass. (Issue 3) `MIN_VIEWPORT_SPAN_MS` derived from `TREND_VIEWER_DEFAULTS` defaults — no spec change (§6.3 already correct). (Issue 4) Spec §7.1 file map updated to add `useSessionPersistence.ts`, expand `useTrendMode.ts` for `onTransition`, retitle `TrendChartContainer.tsx` as "thin wiring layer" with ~440-line count; closing "hooks pending" line cleaned up; handoff §7 Container Wiring rewritten to describe the new hook composition order, the `stableTransitionHandler` ref pattern that resolves the declaration-order dependency, the `useEffect`-only schedule-save trigger, and the absence of any `dispatchModeAction` wrapper. (Issue 5) Dead-code deletions (`pruneAndAdd`, `MAX_ACTIVE_TILES`, `synthesizeNullTile`, `SpanBucketIndicator.tsx`) — spec §7.1 `SpanBucketIndicator` row dropped; handoff §2 row dropped (in Issue 10 propagation). (Issue 7) Spec §19 `latestSampleTs` simplified from `max(hwm, currentMaxAcrossSubscribedTags)` to just `sessionHighWaterMark` with historical note. (Issues 9 + 10) all direct spec/handoff edits already landed in-session — §6.1 wording, §7.1 axisInteractions 9→7 count, handoff §2 file-map re-indent + stale-row cleanup, handoff §1 `commitAndDrain` rename, §19 Live Mode three-uses expansion. Handoff §1 test count synced: 815 → 797 in `@caro/trend-chart` (+6 onTransition tests, −24 deleted test blocks, 3 in-place updates for HWM-only). Spec bumped to v2.4. `Docs/hmi_trend_viewer_audit_2026-05-27.md` retained as the audit's source-of-truth artifact.
