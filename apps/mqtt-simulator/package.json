{
  "name": "@caro/mqtt-simulator",
  "version": "1.0.0",
  "description": "CARO Platform MQTT Simulator — dev tool only, not for production",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --ext js --watch src src/index.js"
  },
  "dependencies": {
    "@caro/db": "*",
    "express": "^4.18.0",
    "mqtt": "^5.0.0",
    "protobufjs": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
